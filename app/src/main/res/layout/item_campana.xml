<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/rounder_backgroundcampana">

    <!-- Contenedor de textos con padding interno -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/textContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/imgcampana">

        <!-- Título -->
        <TextView
            android:id="@+id/tvTituloCampana"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Campaña"
            android:textColor="@color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Especialidad médica -->
        <TextView
            android:id="@+id/tvMedicaCampana"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Medicina"
            android:textColor="@color/white"
            android:textSize="18sp"
            app:layout_constraintTop_toBottomOf="@id/tvTituloCampana"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Mensaje final -->
        <TextView
            android:id="@+id/tvLimiteCampana"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Solo por este mes"
            android:textColor="@color/white"
            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@id/tvMedicaCampana"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Botón Ver -->
        <Button
            android:id="@+id/btnVerCampana"
            android:layout_width="90dp"
            android:layout_height="36dp"
            android:layout_marginTop="5dp"
            android:backgroundTint="@color/white"
            android:text="Ver"
            android:textColor="#5991a5"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/tvLimiteCampana"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Imagen sin padding, pegada a los bordes -->
    <ImageView
        android:id="@+id/imgcampana"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        android:src="@drawable/mujeranuncio"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/textContainer"
        app:layout_constraintWidth_percent="0.50" />

</androidx.constraintlayout.widget.ConstraintLayout>
